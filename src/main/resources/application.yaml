spring:
  application:
    name: laboratorio2

  datasource:
    url: jdbc:postgresql://localhost:5432/{BD_NAME}
    username: {USERNAME}
    password: [PASSWORD]

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect